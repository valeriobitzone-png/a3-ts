# GitHub metadata

Suggested description: TypeScript reference implementation of the A3-EP protocol and lock-vector tests.

Suggested topics: `typescript`, `epistemic-protocol`, `cloud-events`, `jcs`, `conformance`.

These are manual GitHub settings; no remote metadata is changed by this repository.