{
  "name": "a3-ts",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "TypeScript reference implementation of A3-EP v0.2.0 with lock v2 (JCS, CloudEvents io.a3ep.*, temporal, truth, confidence).",
  "scripts": {
    "test": "node --test --test-reporter=spec test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
