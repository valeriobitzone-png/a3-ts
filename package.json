{
  "name": "a3-ts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TypeScript reference implementation of the a3 core contract (JCS, CloudEvents, temporal, truth, confidence).",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
